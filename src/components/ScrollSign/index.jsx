import React from 'react';
import styles from './ScrollSign.module.scss'

const ScrollSign = (props) => (
      <div className={styles.scroll}>
        <span className={styles.text}>scroll</span>
      </div>
  );

export default ScrollSign;